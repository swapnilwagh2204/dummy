version: "3"

services:
    web:
        build: .
        command: python manage.py runserver 0.0.0.0:8000
        ports:
            - "8001:8000"
            
# aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 390618173518.dkr.ecr.us-east-1.amazonaws.com

